# Supabase Edge Functions

Place Deno edge functions here. Each function lives in its own subdirectory:

```
functions/
  hello/
    index.ts
```

Deploy with: `supabase functions deploy <name>`

Engineering Phase 1: no functions implemented yet.
